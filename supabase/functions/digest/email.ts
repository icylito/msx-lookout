// local-dev stub only: the deploy bundles the real ../../../email.ts as a sibling file,
// so this file is never actually deployed. It exists purely so `./email.ts` resolves
// for local type-checking / the IDE.
export * from "../../../email.ts";
