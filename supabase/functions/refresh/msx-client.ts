// local-dev stub only: the deploy script uploads the real ../../../msx-client.ts as a
// sibling file in the deployed bundle, so this file is never actually deployed.
// It exists purely so `./msx-client.ts` resolves for local type-checking / the IDE.
export * from "../../../msx-client.ts";
