// local-dev stub only: the deploy script uploads the real ../../../stockanalysis.ts as a
// sibling file in the deployed bundle, so this file is never actually deployed.
// It exists purely so `./stockanalysis.ts` resolves for local type-checking / the IDE.
export * from "../../../stockanalysis.ts";
